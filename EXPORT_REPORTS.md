# Экспорт и просмотр отчётов пациентов

## 📋 Обзор

Реализован полный функционал для просмотра и экспорта отчётов опросов пациентов в удобочитаемом формате.

## 🎯 Возможности

### 1. **Просмотр отчёта в админ-панели**

#### В списке сессий:
- Для каждой **завершённой** сессии отображаются кнопки:
  - 🔵 **Просмотр** — открыть отчёт в новой вкладке
  - 🔴 **PDF** — скачать отчёт в формате PDF
  - 🟢 **TXT** — скачать отчёт в текстовом формате

#### На странице детальной информации:
- **Встроенный предпросмотр отчёта** в iframe
- Кнопка "Открыть в новом окне" для полноэкранного просмотра

### 2. **Форматы экспорта**

#### PDF (рекомендуется)
- Профессиональное оформление
- Готов для печати и отправки
- Сохраняет все стили и структуру

#### TXT (текстовый)
- Простой формат для копирования
- Совместим с любыми текстовыми редакторами
- Минимальный размер файла

#### HTML
- Для просмотра в браузере
- Можно сохранить через "Сохранить страницу как..."

## 🏥 Структура отчёта

Отчёт генерируется согласно медицинским стандартам и включает:

### 1. **Заголовок**
- ФИО пациента
- Дата прохождения опроса

### 2. **Основная причина обращения** 📌
- Главная жалоба пациента

### 3. **Характеристика боли** 🩺 (если применимо)
- Локализация
- Интенсивность (шкала 1-10)

### 4. **Скрининг систем организма** 🔍
- **Только положительные находки** (принцип "positive findings")
- Разделы по системам:
  - 🫁 Дыхательная система
  - ❤️ Сердечно-сосудистая система
  - 🍽️ Пищеварительная система
  - 🧠 Неврология
  - 💧 Мочевыделительная система

### 5. **Факторы риска** 💊 (Anamnesis Vitae)
- Аллергии
- Семейный анамнез
- Вредные привычки (курение и т.д.)

### 6. **Системный анализ для врача** 🚨
- Автоматические алерты о рисках:
  - ⚠️ Риск ХОБЛ (индекс курильщика)
  - ⚠️ Кардиоваскулярный риск
  - ⚠️ Гастропатология
  - ❗ Онконастороженность
- Рекомендации по дополнительным обследованиям

## 🔧 API Endpoints

### Предпросмотр
```
GET /api/v1/reports/{session_id}/preview
```
Возвращает HTML-документ для отображения в браузере.

### Экспорт в PDF
```
GET /api/v1/reports/{session_id}/export/pdf
```
Возвращает PDF-файл для скачивания.

### Экспорт в TXT
```
GET /api/v1/reports/{session_id}/export/txt
```
Возвращает текстовый файл для скачивания.

### Экспорт в HTML
```
GET /api/v1/reports/{session_id}/export/html
```
Возвращает HTML-файл для скачивания.

## 📦 Установка зависимостей

Для генерации PDF требуется библиотека WeasyPrint:

```bash
cd backend
pip install -r requirements.txt
```

**Важно для Windows:**
WeasyPrint требует GTK3. Установка:
```bash
# Через Chocolatey
choco install gtk3-runtime

# Или скачать вручную:
# https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer
```

**Для Docker:**
Зависимости установятся автоматически при сборке образа.

## 🎨 Визуальное оформление

### HTML/PDF отчёты:
- Современный дизайн с использованием системных шрифтов
- Разделение на блоки с цветовым кодированием
- Бейджи для важных значений (интенсивность боли, риски)
- Адаптивная вёрстка
- Поддержка печати

### Текстовые отчёты:
- Читаемая структура с отступами
- Эмодзи для визуального разделения блоков
- Подходит для копирования в медицинскую карту

## 🔐 Безопасность

- Отчёты доступны **только для завершённых сессий**
- Требуется аутентификация в админ-панели
- PII не хранится в логах
- Отчёты генерируются "на лету" без промежуточного хранения

## 💡 Примеры использования

### Для администратора:
1. Зайти в админ-панель: `/admin`
2. Открыть "Сессии опросов"
3. Найти нужную завершённую сессию
4. Нажать "Просмотр" или скачать PDF/TXT

### Для врача (через Битрикс24):
- Отчёт автоматически отправляется в Битрикс24 при завершении опроса
- Отображается в timeline сделки/лида

### Программный доступ:
```python
import httpx

session_id = "uuid-сессии"
response = httpx.get(f"http://localhost:8000/api/v1/reports/{session_id}/export/pdf")

with open("report.pdf", "wb") as f:
    f.write(response.content)
```

## 🐛 Возможные проблемы

### PDF не генерируется
- Проверьте установку GTK3 (Windows)
- Проверьте логи: `docker compose logs backend`

### Отчёт не отображается
- Убедитесь, что сессия имеет статус "completed"
- Проверьте наличие ответов в базе данных

### Кириллица отображается некорректно
- WeasyPrint автоматически использует системные шрифты
- В Docker используются шрифты DejaVu (поддержка кириллицы)

## 📝 TODO (будущие улучшения)

- [ ] Возможность выбора языка отчёта (RU/EN)
- [ ] Настройка дизайна отчёта через админ-панель
- [ ] Экспорт в DOCX формат
- [ ] Пакетное скачивание отчётов за период
- [ ] Добавление логотипа клиники в отчёт
- [ ] Email-рассылка отчётов
- [ ] Цифровая подпись PDF

## 📞 Контакты

При возникновении проблем обращайтесь к разработчикам проекта.
