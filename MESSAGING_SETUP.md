# 📲 Автоматическая отправка ссылок на опрос

Этот модуль позволяет автоматически отправлять ссылки на опрос пациентам через различные каналы:
- WhatsApp (через Открытые линии Битрикс24)
- SMS (через провайдеров: SMS.RU, SMSC.RU, UniSender)
- Email
- Telegram Bot

## 🏗 Архитектура

```
Битрикс24 CRM (Сделка создана)
    ↓
[Робот/БП] → POST /api/v1/bitrix/webhook
    ↓
Backend генерирует JWT-токен + Magic Link
    ↓
Backend записывает ссылку в поле UF_CRM_OPROS_LINK
    ↓
[Пауза 5-10 сек]
    ↓
[Робот отправки] → WhatsApp/SMS/Email/Telegram
    ↓
Пациент получает сообщение со ссылкой
```

## 📋 Требования

### Для всех каналов:
- Активная конфигурация опроса в системе
- Настроенный исходящий вебхук Битрикс24 (`BITRIX24_WEBHOOK_URL`)
- Токен безопасности (`BITRIX24_INCOMING_TOKEN`)

### Для WhatsApp:
- Подключенный канал WhatsApp в Открытых линиях Битрикс24
- Тариф: Базовый и выше

### Для SMS:
- Установленное приложение SMS-провайдера из Маркета Битрикс24
- Настроенный API-ключ провайдера
- Баланс на счёте провайдера

### Для Telegram:
- Созданный бот через @BotFather
- `TELEGRAM_BOT_TOKEN` в `.env`
- Поле `UF_CRM_TELEGRAM_ID` в карточке контакта

### Для сокращения ссылок (опционально):
- `BITLY_ACCESS_TOKEN` (для Bit.ly) или использование clck.ru (бесплатно)

## 🚀 Быстрый старт

### Шаг 1: Создание пользовательского поля в Битрикс24

1. Перейдите в **CRM → Настройки → Настройка полей**
2. Выберите **Сделка**
3. Добавьте поле:
   - Название: `Ссылка на опрос`
   - Код: `UF_CRM_OPROS_LINK`
   - Тип: `Строка`

### Шаг 2: Настройка переменных окружения

```bash
# backend/.env

# Битрикс24 (обязательно)
BITRIX24_WEBHOOK_URL=https://your-portal.bitrix24.ru/rest/1/xxxxx/
BITRIX24_INCOMING_TOKEN=your_secret_token_here
BITRIX24_CATEGORY_ID=  # Оставьте пустым для всех воронок

# Сокращение URL (опционально для SMS)
URL_SHORTENER_PROVIDER=clckru  # 'clckru' или 'bitly'
BITLY_ACCESS_TOKEN=  # Только если используете bitly

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=  # Токен от @BotFather
```

### Шаг 3: Перезапуск бэкенда

```bash
docker compose restart backend
```

### Шаг 4: Настройка роботов в Битрикс24

#### Робот 1: Генерация ссылки

**Стадия:** Новая (или любая другая)

**Тип:** Вебхук

**URL:** `https://your-domain.ru/api/v1/bitrix/webhook`

**Метод:** `POST`

**Параметры (JSON):**
```json
{
  "lead_id": "{=Document:ID}",
  "patient_name": "{=Document:CONTACT_ID.NAME} {=Document:CONTACT_ID.LAST_NAME}",
  "entity_type": "DEAL",
  "category_id": "{=Document:CATEGORY_ID}",
  "auth_token": "your_secret_token_here"
}
```

#### Робот 2: Пауза

**Длительность:** 5-10 секунд

#### Робот 3: Отправка в WhatsApp

**Кому:** `{=Document:CONTACT_ID.PHONE}`

**Канал:** Ваш канал WhatsApp

**Текст:**
```
Здравствуйте, {=Document:CONTACT_ID.NAME}!

Пожалуйста, пройдите медицинский опрос перед приёмом:
{=Document:UF_CRM_OPROS_LINK}

Это займёт 3-5 минут. Спасибо!
```

## 📱 Настройка различных каналов

### WhatsApp (Открытые линии)

✅ **Преимущества:** Высокая доставляемость, мгновенная доставка, возможность диалога

❌ **Недостатки:** Требуется активный канал, не все пациенты используют WhatsApp

**Настройка:**
1. Подключите WhatsApp в разделе Открытые линии
2. Используйте робота "Отправить сообщение в открытую линию"
3. Убедитесь, что телефон контакта в международном формате (+7XXXXXXXXXX)

### SMS

✅ **Преимущества:** Универсальность (работает на любом телефоне), высокая доставляемость

❌ **Недостатки:** Платная отправка, ограничение по длине (70 символов кириллицы)

**Настройка:**
1. Установите приложение провайдера из Маркета (например, SMS.RU)
2. Настройте API-ключ
3. Используйте робота "Отправить SMS"
4. Рекомендуется использовать сокращатель ссылок (см. ниже)

**Оптимизация для SMS:**

Включите сокращение ссылок в коде:

```python
# В backend/app/api/v1/endpoints/bitrix_webhook.py

from app.services.url_shortener import shorten_url

# После генерации survey_url:
short_url = await shorten_url(survey_url, provider="clckru")

# Записать короткую ссылку:
await bitrix_client.update_deal_field(
    deal_id=lead_id,
    fields={"UF_CRM_OPROS_LINK": short_url}
)
```

### Email

✅ **Преимущества:** Можно отправить красивое HTML-письмо, нет ограничений по длине

❌ **Недостатки:** Может попасть в спам, не все пациенты проверяют почту регулярно

**Настройка:**
1. Используйте робота "Отправить email"
2. Создайте HTML-шаблон с кнопкой-ссылкой
3. Убедитесь, что у контакта заполнено поле Email

### Telegram Bot

✅ **Преимущества:** Бесплатно, мгновенная доставка, богатые возможности форматирования

❌ **Недостатки:** Требуется, чтобы пациент заранее запустил бота, нужно хранить Telegram ID

**Настройка:**

1. Создайте бота через @BotFather
2. Добавьте токен в `.env`:
   ```
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
3. Создайте поле `UF_CRM_TELEGRAM_ID` в контактах
4. Добавьте интеграцию в код:

```python
# В backend/app/api/v1/endpoints/bitrix_webhook.py

from app.services.telegram_notifier import send_survey_link_telegram, get_telegram_id_from_bitrix

# После записи ссылки в поле:
telegram_id = await get_telegram_id_from_bitrix(lead_id)
if telegram_id:
    await send_survey_link_telegram(
        chat_id=telegram_id,
        patient_name=patient_name or "Пациент",
        survey_url=survey_url,
    )
```

## 🧪 Тестирование

### Тест Telegram:

```bash
cd backend
python scripts/test_notifications.py --channel telegram --chat-id YOUR_CHAT_ID
```

### Тест сокращения URL:

```bash
python scripts/test_notifications.py --channel url-shortener --url "https://example.com/very-long-url"
```

### Полный тест:

```bash
python scripts/test_notifications.py --channel all --chat-id YOUR_CHAT_ID
```

## 🔧 Устранение неполадок

### Ссылка не записывается в поле UF_CRM_OPROS_LINK

**Причина:** Поле не создано или имеет неверный код

**Решение:**
1. Проверьте, что поле создано: CRM → Настройки → Настройка полей → Сделка
2. Убедитесь, что код точно `UF_CRM_OPROS_LINK`
3. Проверьте логи: `docker compose logs backend --tail 50`

### WhatsApp/SMS не отправляется

**Причина:** Телефон в неверном формате

**Решение:**
- Формат должен быть международным: `+79991234567`
- Без пробелов, скобок, тире
- С кодом страны (+7 для России)

### Telegram сообщение не доставляется

**Причина:** Неверный chat_id или бот заблокирован пользователем

**Решение:**
1. Убедитесь, что пациент запустил бота (/start)
2. Проверьте chat_id через @userinfobot
3. Проверьте токен бота в .env

### Ссылка слишком длинная для SMS

**Решение:** Включите сокращение URL (см. раздел "SMS - Оптимизация")

## 📚 Дополнительные ресурсы

- [Полная документация интеграции](BITRIX24_INTEGRATION.md)
- [Битрикс24 REST API](https://dev.1c-bitrix.ru/rest_help/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Bit.ly API](https://dev.bitly.com/)

## 🔐 Безопасность

- Не передавайте персональные данные в URL
- Используйте HTTPS для всех ссылок
- Ограничьте срок действия ссылок (настраивается в `JWT_EXPIRATION_HOURS`)
- Получите согласие пациента на отправку сообщений (152-ФЗ)

## 📝 Лицензия

Внутренний проект клиники. Соблюдается 152-ФЗ о персональных данных.
