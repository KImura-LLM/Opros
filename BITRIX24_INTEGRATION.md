# 🔗 Пошаговое руководство по интеграции с Битрикс24

> **Дата обновления:** 13.02.2026
> **Статус:** Актуально

Это подробная инструкция для администраторов, которая поможет связать Опросник и CRM Битрикс24. Мы настроим систему так, чтобы при создании новой сделки пациенту отправлялась ссылка, а после опроса результат попадал в карточку врача.

---

## 📑 Оглавление
1. [Этап 1: Подготовка (Битрикс24) — Создание ключа доступа](#этап-1-подготовка-битрикс24--создание-ключа-доступа)
2. [Этап 2: Настройка сервера (Backend)](#этап-2-настройка-сервера-backend)
3. [Этап 3: Настройка автоматизации (Битрикс24 → Опросник)](#этап-3-настройка-автоматизации-битрикс24--опросник)
4. [Этап 4: Проверка работы](#этап-4-проверка-работы)
5. [Возможные проблемы](#возможные-проблемы)

---

## Этап 1: Подготовка (Битрикс24) — Создание ключа доступа

На этом этапе мы дадим нашему Опроснику разрешение "стучаться" в Битрикс24, чтобы скачивать данные пациента и загружать PDF-отчёты.

1. Зайдите в ваш портал **Битрикс24** под администратором.
2. В левом меню нажмите **«Разработчикам»** (иногда скрыто в «Ещё» или «Приложения»).
3. В открывшемся разделе выберите вкладку **«Другое»**.
4. Нажмите на плитку **«Входящий вебхук»**.
5. Откроется форма настройки вебхука:
   * **Название:** Напишите `Интеграция с Опросником пациента`.
   * **Описание:** `Для загрузки PDF отчётов и комментариев`.
6. Блок **«Настройка прав»** (очень важно!):
   * Нажмите «Выбрать» и отметьте галочками следующие пункты:
     * `CRM` (crm) — для работы со сделками/лидами.
     * `Диск` (disk) — для загрузки PDF файлов.
     * `Списки` (lists) — если используются.
     * `Пользователи` (user) — базовый доступ.
7. Нажмите кнопку **«Сохранить»**.
8. Появится поле **«Вебхук для вызова REST API»**.
   * Скопируйте эту ссылку целиком.
   * Она выглядит так: `https://vash-portal.bitrix24.ru/rest/1/a1b2c3d4e5f6g7h8/`
   * ⚠️ **Никому не передавайте эту ссылку!** Это "ключ от квартиры" вашей CRM.

---

## Этап 2: Настройка сервера (Backend)

Теперь нужно "рассказать" Опроснику, куда отправлять отчеты.

1. Откройте файл `.env` в корне проекта на сервере (или локально).
2. Найдите секцию **Битрикс24 Интеграция**.
3. Заполните переменные данными из Этапа 1:

```dotenv
# Вставьте ссылку, которую вы скопировали на шаге 1.8
BITRIX24_WEBHOOK_URL=https://vash-portal.bitrix24.ru/rest/1/a1b2c3d4e5f6g7h8/

# Придумайте сложный пароль (токен). Его мы будем использовать в Битрикс24,
# чтобы никто чужой не мог генерировать ссылки на опросы.
# Пример: mySecretToken2026SuperSecure
BITRIX24_INCOMING_TOKEN=mySecretToken2026SuperSecure

# (ОПЦИОНАЛЬНО) ID категории воронки ДЛЯ ОБРАБОТКИ
# Если у вас несколько воронок CRM, укажите ID нужной (например, "5").
# Оставьте пустым, если хотите обрабатывать сделки из всех воронок.
BITRIX24_CATEGORY_ID=

# Адрес вашего сайта, где развернут опросник (без слэша в конце)
FRONTEND_URL=https://opros.your-clinic.ru
```

4. Сохраните файл и перезапустите бэкенд:
   ```bash
   docker compose restart backend
   ```

---

## Этап 3: Настройка автоматизации (Битрикс24 → Опросник)

Самая важная часть. Мы настроим, чтобы при попадании сделки в стадию "Новая", Битрикс сам запрашивал ссылку у Опросника.

### Способ А: Через "Роботы" (Простой)
**Внимание:** Стандартные роботы Битрикс24 не всегда умеют читать ответ от сервера (JSON). Если этот способ не сработает, используйте Способ Б.

1. Перейдите в **CRM** → **Сделки**.
2. Переключитесь в режим **Канбан** (доска с колонками).
3. Нажмите кнопку **«Роботы»** (справа сверху).
4. Найдите нужную стадию (например, «Новая»). Нажмите **«+ Добавить»**.
5. Выберите **«Свои роботы»** → **«Вебхук»** или **«Исходящий вебхук»** (название зависит от версии Битрикс24).
6. В открывшемся окне заполните:
   * **URL:** `https://opros.your-clinic.ru/api/v1/bitrix/webhook` (замените домен на ваш).
   * **Метод:** `POST`.
   * **Тело запроса (JSON):** Скопируйте и вставьте этот код:

   ```json
   {
       "lead_id": "{{ID}}",
       "patient_name": "{{CONTACT_NAME}} {{CONTACT_LAST_NAME}}",
       "entity_type": "DEAL",
       "category_id": "{{CATEGORY_ID}}",
       "auth_token": "mySecretToken2026SuperSecure"
   }
   ```
   *Замените `mySecretToken2026SuperSecure` на то, что вы написали в `.env`.*

7. Нажмите **«Сохранить»**.

> Если в вашем интерфейсе есть только поле **«Хендлер»** (без отдельных полей URL/Метод/JSON), вставьте в него URL целиком с параметрами:
>
> `https://opros.your-clinic.ru/api/v1/bitrix/webhook?lead_id={{ID}}&patient_name={{CONTACT_NAME}}%20{{CONTACT_LAST_NAME}}&entity_type=DEAL&category_id={{CATEGORY_ID}}&auth_token=mySecretToken2026SuperSecure`
>
> Замените `mySecretToken2026SuperSecure` на значение `BITRIX24_INCOMING_TOKEN` из `.env`.

### Способ Б: Через "Бизнес-процессы" (Надежный, для тарифов Профессиональный/Коробка)

Этот способ позволяет гарантированно получить ссылку и отправить её в WhatsApp/SMS.

1. Перейдите в **CRM** → **Настройки** → **Роботы и бизнес-процессы** → **Бизнес-процессы**.
2. Выберите **«Сделка»** → **«Добавить шаблон»**.
3. Назовите: `Генерация ссылки на опрос`. Галочка: **«При добавлении»**.
4. Откроется дизайнер. В правой панели найдите **«Действия приложений»** (или «Прочее»).
5. Перетащите блок **«Вебхук»** (Webhook) в схему.
6. Нажмите на шестеренку (настройки) блока.

#### Вариант 6.1: Если есть отдельные поля (URL, Метод, JSON)
   * **URL:** `https://opros.your-clinic.ru/api/v1/bitrix/webhook`
   * **Метод:** `POST`
   * **Тип контента:** `JSON`
   * **Параметры:** (добавляем вручную строками или JSON)
     ```json
     {
       "lead_id": "{=Document:ID}",
       "patient_name": "{=Document:CONTACT_ID.NAME} {=Document:CONTACT_ID.LAST_NAME}",
       "entity_type": "DEAL",
       "category_id": "19",
       "auth_token": "mySecretToken2026SuperSecure"
     }
     ```
   * **Сохранять результат:** Да (если доступно).

#### Вариант 6.2: Если есть только поле "Хендлер" (ваш случай)
   Вставьте в поле **«Хендлер»** следующую строку целиком:
   ```
   https://opros-izdorov.ru/api/v1/bitrix/webhook?lead_id={=Document:ID}&patient_name={=Document:CONTACT_ID.NAME}%20{=Document:CONTACT_ID.LAST_NAME}&entity_type=DEAL&category_id=19&auth_token=245234asdufhb9oas94
   ```
   
   ⚠️ **Важно:**
   * Замените `auth_token=245234asdufhb9oas94` на значение `BITRIX24_INCOMING_TOKEN` из вашего `.env` файла.
   * `category_id=19` — означает, что обрабатываться будут **только сделки из воронки №19**. Если нужна другая воронка, измените число.
   * Пробел между именем и фамилией кодируется как `%20`.

7. **Как отправить ссылку пациенту?**
   * *Вариант 1:* Если Опросник настроен обновлять поле в сделке, просто добавьте паузу 1 мин, а потом отправку SMS с содержимым поля `UF_OPROS_LINK` (нужно создать такое поле в CRM заранее).
   * *Вариант 2:* (Для разработчиков) Опросник возвращает JSON. Стандартными средствами разобрать JSON сложно. Рекомендуется доработать API, чтобы Опросник **сам** записывал ссылку в нужное поле сделки через обратный вызов.

> **Рекомендация:** Для стабильности мы рекомендуем, чтобы **бэкенд сам записывал ссылку в поле сделки**.
> Если вы используете текущую версию API, она возвращает ссылку в ответе. Чтобы её использовать, нужен платный Activity из Маркета "JSON Parser" или PHP-код (в коробке).

---

## Этап 4: Проверка работы

1. Создайте **новую сделку** в Битрикс24.
   * Укажите Имя контакта (пациента).
   * Убедитесь, что сделка попала в стадию, где настроен Робот/БП.
2. Подождите 5-10 секунд.
3. Проверьте:
   * **В Битрикс24:** Если настроена отправка сообщения, оно должно уйти. Если вы тестируете просто создание — откройте логи Робота (кнопка «Роботы» → вкладка «История»). Там должно быть написано `Успешно`.
   * **На сервере:** Посмотрите логи контейнера:
     ```bash
     docker compose logs backend --tail 20
     ```
     Вы должны увидеть строки:
     `INFO ... Получен вебхук от Битрикс24 ...`
     `INFO ... Сгенерирована ссылка для опроса ...`

4. Скопируйте ссылку (из логов или ответа) и пройдите опрос до конца.
5. После завершения вернитесь в **карточку сделки** в Битрикс24.
   * В **Таймлайне** (правая часть) должен появиться комментарий "Пациент завершил опрос...".
   * На вкладке **«Дела»** (или Файлы) должен появиться PDF отчет.

---

## Возможные проблемы

### 1. Робот выдает ошибку "401 Unauthorized"
* **Причина:** Токен в роботе не совпадает с `BITRIX24_INCOMING_TOKEN`.
* **Решение:** Проверьте `.env` файл и настройки робота. Они должны быть идентичны.

### 2. PDF не появляется в сделке
* **Причина:** Неверные права у вебхука (Этап 1) или неверный URL в `.env` (Этап 2).
* **Решение:**
  1. Зайдите в Битрикс24 → Вебхуки.
  2. Откройте ваш вебхук.
  3. Проверьте права: `CRM`, `Диск` должны быть выбраны.
  4. Проверьте логи бэкенда — там будет точная ошибка (например, `Access denied`).

### 3. Ссылка генерируется, но имя пациента "Unknown"
* **Причина:** В роботе неправильно указаны переменные.
* **Решение:** В JSON робота используйте `{{CONTACT_NAME}}`, а не просто `{{NAME}}` (последнее — это название сделки, а не имя человека).

### 4. В ответе вебхука приходит "Error parsing data"
* **Причина:** Битрикс24 отправил данные как Form-Data, а сервер ждал JSON (или наоборот).
* **Решение:** Код сервера поддерживает оба формата, но лучше в роботе явно выбирать тип `JSON`.

### 5. Ошибка "403 Forbidden: Эта воронка не обрабатывается"
* **Причина:** В `.env` указан `BITRIX24_CATEGORY_ID`, но робот сработал для сделки из другой воронки.
* **Решение:**
  1. **Чтобы обрабатывать только одну воронку:** Перейдите в Битрикс24 → CRM → Сделки → Выберите нужную воронку. В URL браузера найдите `category_id=X`. Это значение `X` укажите в `BITRIX24_CATEGORY_ID` в `.env`.
  2. **Чтобы обрабатывать все воронки:** Оставьте `BITRIX24_CATEGORY_ID` пустым в `.env`.
  3. **Если в роботе нет `{{CATEGORY_ID}}`:** Добавьте это поле в тело запроса (см. Этап 3).

---

## Этап 5: Автоматическая отправка ссылки на опрос через SMS/Мессенджеры 📱

Теперь настроим отправку сгенерированной ссылки пациенту автоматически. Существует несколько способов в зависимости от вашего тарифа Битрикс24.

---

### 📱 Способ 1: Отправка через WhatsApp (Открытые линии)

**Требования:** Подключенный канал WhatsApp в Открытых линиях.

#### Шаг 1: Создание пользовательского поля для хранения ссылки

1. Зайдите в **CRM** → **Настройки** → **Настройка полей**.
2. Выберите сущность **«Сделка»** (или «Лид»).
3. Нажмите **«Добавить пользовательское поле»**:
   * **Название:** `Ссылка на опрос`
   * **Код:** `UF_CRM_OPROS_LINK`
   * **Тип:** `Строка`
   * **Множественное:** Нет
4. Сохраните поле.

#### Шаг 2: Доработка API для записи ссылки в поле сделки

Добавьте в код бэкенда автоматическую запись ссылки в созданное поле:

```python
# В файле backend/app/api/v1/endpoints/survey.py
# После генерации ссылки добавьте обновление поля:

async def update_deal_field(lead_id: int, survey_link: str):
    """Обновление пользовательского поля в сделке с ссылкой на опрос"""
    from app.services.bitrix24 import Bitrix24Client
    
    client = Bitrix24Client()
    webhook_url = settings.BITRIX24_WEBHOOK_URL
    
    if not webhook_url:
        return
    
    method_url = f"{webhook_url.rstrip('/')}/crm.deal.update"
    
    payload = {
        "id": lead_id,
        "fields": {
            "UF_CRM_OPROS_LINK": survey_link
        }
    }
    
    async with httpx.AsyncClient(timeout=30.0) as http_client:
        response = await http_client.post(method_url, json=payload)
        if response.status_code == 200:
            logger.info(f"Ссылка записана в поле сделки {lead_id}")
        else:
            logger.error(f"Ошибка записи ссылки: {response.text}")
```

#### Шаг 3: Настройка робота отправки в WhatsApp

1. Перейдите в **CRM** → **Сделки** → **Роботы**.
2. На стадии **«Новая»** (после робота генерации ссылки) добавьте **паузу** на 5-10 секунд.
3. После паузы добавьте робота **«Отправить сообщение в WhatsApp»** (или «Отправить в Открытую линию»):
   * **Кому:** `{=Document:CONTACT_ID.PHONE}` (телефон контакта)
   * **Канал:** Выберите ваш канал WhatsApp
   * **Текст сообщения:**
   ```
   Здравствуйте, {=Document:CONTACT_ID.NAME}!
   
   Пожалуйста, пройдите медицинский опрос перед приёмом:
   {=Document:UF_CRM_OPROS_LINK}
   
   Это займёт 3-5 минут. Спасибо!
   ```

4. Сохраните робота.

---

### 📧 Способ 2: Отправка через SMS (СМС-центр / Провайдеры)

**Требования:** Подключенный SMS-провайдер в Битрикс24.

#### Популярные провайдеры:
- **SMS.RU** (есть готовое приложение в Маркете)
- **SMSC.RU** 
- **UniSender**
- **Twilio** (для международной отправки)

#### Настройка отправки SMS:

1. Установите приложение провайдера из **Маркета** Битрикс24.
2. Следуйте инструкциям провайдера для подключения API.
3. После генерации ссылки (с паузой 5-10 сек) добавьте робота **«Отправить SMS»**:
   * **Номер получателя:** `{=Document:CONTACT_ID.PHONE}`
   * **Текст:**
   ```
   Здравствуйте! Пройдите опрос перед приёмом: {=Document:UF_CRM_OPROS_LINK}
   ```

**⚠️ Важно:** SMS имеют ограничение по длине (160 символов для латиницы, 70 для кириллицы). Рекомендуем использовать сокращатель ссылок (bit.ly, clck.ru) или встроенный в Битрикс24.

---

### 📨 Способ 3: Отправка через Email

1. На стадии после генерации ссылки добавьте робота **«Отправить email»**.
2. Настройте шаблон письма:
   * **Кому:** `{=Document:CONTACT_ID.EMAIL}`
   * **Тема:** `Подготовка к приёму — анкета пациента`
   * **Текст (HTML):**
   ```html
   <p>Уважаемый(ая) {=Document:CONTACT_ID.NAME} {=Document:CONTACT_ID.LAST_NAME}!</p>
   
   <p>Для подготовки к вашему визиту просим заполнить медицинскую анкету.</p>
   
   <p><a href="{=Document:UF_CRM_OPROS_LINK}" style="display:inline-block; padding:12px 24px; background:#0066CC; color:#fff; text-decoration:none; border-radius:4px;">Перейти к опросу</a></p>
   
   <p>Это займёт не более 5 минут.</p>
   
   <p>С уважением,<br>Команда клиники</p>
   ```

---

### 🔗 Способ 4: Использование сокращателя ссылок (для SMS)

Для экономии символов в SMS можно интегрировать сервис сокращения URL.

#### Вариант А: Использование bit.ly через API

Добавьте в ваш бэкенд функцию сокращения:

```python
# backend/app/services/url_shortener.py

import httpx
from app.core.config import settings

async def shorten_url(long_url: str) -> str:
    """Сокращение URL через bit.ly API"""
    
    if not settings.BITLY_ACCESS_TOKEN:
        return long_url  # Возвращаем оригинал, если токен не настроен
    
    api_url = "https://api-ssl.bitly.com/v4/shorten"
    headers = {
        "Authorization": f"Bearer {settings.BITLY_ACCESS_TOKEN}",
        "Content-Type": "application/json"
    }
    
    payload = {
        "long_url": long_url
    }
    
    async with httpx.AsyncClient() as client:
        response = await client.post(api_url, json=payload, headers=headers)
        
        if response.status_code == 200:
            data = response.json()
            return data.get("link", long_url)
        
        return long_url  # В случае ошибки возвращаем оригинал
```

Затем используйте эту функцию при генерации ссылки перед записью в поле `UF_CRM_OPROS_LINK`.

#### Вариант Б: Использование clck.ru (бесплатно, без регистрации)

```python
async def shorten_url_clckru(long_url: str) -> str:
    """Сокращение через clck.ru (без API-ключа)"""
    api_url = "https://clck.ru/--"
    
    params = {"url": long_url}
    
    async with httpx.AsyncClient() as client:
        response = await client.get(api_url, params=params)
        
        if response.status_code == 200:
            return response.text.strip()
        
        return long_url
```

---

### 🚀 Способ 5: Telegram бот (расширенный вариант)

Если у вас есть разработчик, можно создать Telegram-бота для отправки:

1. Создайте бота через [@BotFather](https://t.me/BotFather).
2. Сохраните токен бота.
3. Добавьте в бэкенд функцию отправки:

```python
# backend/app/services/telegram_notifier.py

import httpx
from app.core.config import settings

async def send_telegram_message(chat_id: str, text: str):
    """Отправка сообщения через Telegram Bot API"""
    
    if not settings.TELEGRAM_BOT_TOKEN:
        return
    
    api_url = f"https://api.telegram.org/bot{settings.TELEGRAM_BOT_TOKEN}/sendMessage"
    
    payload = {
        "chat_id": chat_id,
        "text": text,
        "parse_mode": "HTML"
    }
    
    async with httpx.AsyncClient() as client:
        await client.post(api_url, json=payload)
```

4. В карточке контакта в Битрикс24 создайте поле `UF_TELEGRAM_ID` для хранения Telegram ID пациента.
5. При генерации ссылки вызывайте функцию отправки в Telegram.

---

### 📊 Рекомендуемая последовательность роботов

Для полной автоматизации выстройте цепочку роботов на стадии **«Новая»**:

1. **Робот:** Вебхук генерации ссылки → Опросник создаёт сессию и записывает ссылку в `UF_CRM_OPROS_LINK`.
2. **Пауза:** 5-10 секунд (чтобы API успело обновить поле).
3. **Условие:** Проверка, что поле `UF_CRM_OPROS_LINK` не пустое.
4. **Робот:** Отправка сообщения:
   * Если есть WhatsApp → отправить в WhatsApp.
   * Если нет WhatsApp, но есть телефон → отправить SMS.
   * Если ни того, ни другого → отправить Email.
5. **Уведомление:** (опционально) Уведомить ответственного менеджера в чат.

---

### ❗ Распространённые проблемы при отправке

#### 1. Сообщение не отправляется в WhatsApp/SMS
* **Причина:** Не заполнено поле телефона у контакта или номер в неверном формате.
* **Решение:** 
  1. Убедитесь, что у контакта заполнено поле **«Телефон»**.
  2. Формат должен быть международным: `+79991234567` (с плюсом и кодом страны).
  3. Проверьте, что канал WhatsApp/SMS активен и подключен.

#### 2. Поле UF_CRM_OPROS_LINK пустое
* **Причина:** Бэкенд не обновляет поле сделки автоматически.
* **Решение:** Убедитесь, что в коде добавлена функция `update_deal_field()` из Шага 2 способа 1, и она вызывается после генерации ссылки.

#### 3. Ссылка в SMS обрезается или не кликабельна
* **Причина:** Длинная ссылка превышает лимит SMS или не распознаётся телефоном.
* **Решение:** Используйте сокращатель ссылок (Способ 4).

#### 4. Открытая линия WhatsApp не находит контакт
* **Причина:** Телефон в CRM указан в другом формате, чем зарегистрирован в WhatsApp.
* **Решение:** Приведите все телефоны к единому формату: `+7XXXXXXXXXX` (без пробелов, скобок, тире).

---

## 🔐 Безопасность и соответствие 152-ФЗ

При отправке ссылок через мессенджеры и SMS важно:

1. **Не передавать в URL персональные данные пациента.** Используйте только уникальный токен сессии.
2. **Использовать HTTPS** для всех ссылок.
3. **Ограничить срок действия ссылки** (рекомендуется 72 часа). Настраивается в `backend/.env`:
   ```
   SURVEY_LINK_EXPIRY_HOURS=72
   ```
4. **Получить согласие на отправку сообщений.** Убедитесь, что у пациента запрошено согласие на получение SMS/WhatsApp в рамках обработки персональных данных.
5. **Хранить логи отправки** для аудита (если требуется по внутренним политикам).

---

## 📚 Дополнительные ресурсы

* [Документация Битрикс24 REST API](https://dev.1c-bitrix.ru/rest_help/)
* [Открытые линии и мессенджеры](https://helpdesk.bitrix24.ru/open/18034090/)
* [SMS-центры в Битрикс24](https://helpdesk.bitrix24.ru/open/6870831/)
* [Bit.ly API документация](https://dev.bitly.com/)
* [Telegram Bot API](https://core.telegram.org/bots/api)
