# 🚀 Пошаговая инструкция по деплою «Опросник пациента»

> **Дата:** Февраль 2026  
> **Исходные данные:** Сервер на Timeweb Cloud (уже создан) + домен на Reg.ru (уже куплен)  
> **Стек:** FastAPI + React + PostgreSQL + Redis + Nginx + Docker  

---

## 📋 Оглавление

| № | Этап | Время |
|---|------|-------|
| 1 | [Подключение к серверу](#этап-1--подключение-к-серверу) | 2 мин |
| 2 | [Подготовка сервера](#этап-2--подготовка-сервера) | 5 мин |
| 3 | [Установка Docker](#этап-3--установка-docker) | 5 мин |
| 4 | [Привязка домена (DNS)](#этап-4--привязка-домена-dns-на-regru) | 3 мин |
| 5 | [Загрузка проекта на сервер](#этап-5--загрузка-проекта-на-сервер) | 5 мин |
| 6 | [Создание .env файла](#этап-6--создание-env-файла) | 3 мин |
| 7 | [Настройка Nginx под домен](#этап-7--настройка-nginx-под-домен) | 2 мин |
| 8 | [Получение SSL-сертификата](#этап-8--получение-ssl-сертификата) | 5 мин |
| 9 | [Запуск проекта](#этап-9--запуск-проекта) | 5 мин |
| 10 | [Проверка работоспособности](#этап-10--проверка-работоспособности) | 3 мин |

**⏱ Общее время:** ~40 минут

---

## 🔑 Готовые данные для вставки

Ниже — **предгенерированные** пароли и ключи. Используйте их на соответствующих шагах.  
⚠️ **Сохраните эту таблицу в надёжном месте** (менеджер паролей, зашифрованный файл).

| Параметр | Готовое значение | Где используется |
|----------|------------------|------------------|
| Пароль пользователя `deploy` | `Xk9$mWr4vLp2nBq7` | Шаг 2.2 |
| `SECRET_KEY` | `a3f8c1e9d4b7260f5e1a9c3d8b4f7e2a6d0c5b8e1f3a7d9c2b6e4f8a0d3c7b1` | .env файл |
| `JWT_SECRET_KEY` | `7b2e4f6a8d0c3e5b1a9f7d4c6b8e2a0f3d5c7b9e1a4f6d8c0b2e4a6f8d0c3e5` | .env файл |
| `POSTGRES_PASSWORD` | `Pg_secur3_Opr0s_2026xQ` | .env файл |
| `REDIS_PASSWORD` | `R3d1s_Secur3_0pr0s_vK` | .env файл |
| `ADMIN_USERNAME` | `opros_admin` | .env файл |
| `ADMIN_PASSWORD` | `Adm1n_0proS_2026wZ` | .env файл |

> 📌 **Хотите свои пароли?** Выполните на сервере после подключения:
> ```bash
> openssl rand -hex 32    # для SECRET_KEY и JWT_SECRET_KEY
> openssl rand -base64 20 # для остальных паролей
> ```

---

## Этап 1 — Подключение к серверу

### Что нужно перед началом

- [+] IP-адрес сервера из панели Timeweb Cloud (формат: `185.104.xxx.xxx`)
- [+] Пароль root (пришёл на email при создании сервера)

### Где найти IP-адрес и пароль root

1. Откройте **https://cloud.timeweb.com** → войдите в аккаунт
2. В левом меню нажмите **«Облачные серверы»**
3. Нажмите на **имя вашего сервера**
4. На странице сервера найдите:
   - **IP-адрес** — в блоке «Сеть» (формат: `xxx.xxx.xxx.xxx`)
   - **Пароль root** — нажмите **«Показать пароль»** (иконка 👁)

---

### Шаг 1.1 — Подключиться по SSH

- [+] **Выполнено**

Откройте **терминал** (PowerShell / Cursor Terminal / Windows Terminal) и введите:

```bash
ssh root@147.45.249.254
```

**Что произойдёт:**

Первый вопрос — подтверждение подключения:
```
The authenticity of host '...' can't be established.
Are you sure you want to continue connecting (yes/no)?
```
→ Напечатайте **`yes`** → нажмите **Enter**

Второй вопрос — пароль:
```
root@185.104.123.456's password:
```
→ Вставьте пароль root → нажмите **Enter**
> ⚠️ При вводе пароля символы **не отображаются** — это нормально!

**✅ Результат:** Появится приглашение:
```
root@имя-сервера:~#
```
Вы внутри сервера. Переходите к следующему этапу.

---

## Этап 2 — Подготовка сервера

### Шаг 2.1 — Обновить систему

- [+] **Выполнено**

```bash
apt update && apt upgrade -y
```

> 📌 Скачивает и устанавливает все обновления безопасности. Флаг `-y` = автоматическое «да».

**⏱ Ожидание:** 1–3 минуты.

**✅ Результат:** Вернулось приглашение `root@...:~#` без ошибок.

---

### Шаг 2.2 — Создать пользователя `deploy`

- [+] **Выполнено**

> ⚠️ Работать на сервере от `root` — небезопасно. Создаём отдельного пользователя.

```bash
adduser deploy
```

**Что появится и что вводить:**

| Запрос на экране | Что делать |
|------------------|------------|
| `Enter new UNIX password:` | Введите: `Xk9$mWr4vLp2nBq7` (символы не видны) |
| `Retype new UNIX password:` | Повторите тот же пароль |
| `Full Name []:` | Просто нажмите **Enter** |
| `Room Number []:` | Просто нажмите **Enter** |
| `Work Phone []:` | Просто нажмите **Enter** |
| `Home Phone []:` | Просто нажмите **Enter** |
| `Other []:` | Просто нажмите **Enter** |
| `Is the information correct? [Y/n]` | Нажмите **Y** → **Enter** |

Дайте пользователю права администратора:

```bash
usermod -aG sudo deploy
```

**✅ Результат:** Команда выполнится молча — это нормально.

---

### Шаг 2.3 — Настроить файрвол

- [+] **Выполнено**

Вставьте **все 5 команд** разом:

```bash
apt install ufw -y
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

При вопросе `Proceed with operation (y|n)?` → введите **`y`** → **Enter**

**Проверка:**

```bash
ufw status
```

**✅ Результат:**
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

---

### Шаг 2.4 — Переключиться на пользователя `deploy`

- [+] **Выполнено**

```bash
su - deploy
```

**✅ Результат:** Приглашение сменилось на:
```
deploy@имя-сервера:~$
```

> 📌 С этого момента **все команды** выполняются от пользователя `deploy`.

---

## Этап 3 — Установка Docker

### Шаг 3.1 — Установить Docker Engine

- [+] **Выполнено**

Скопируйте **весь блок** и вставьте в терминал:

```bash
sudo apt install -y ca-certificates curl gnupg lsb-release
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

> 📌 Система запросит пароль `deploy` → введите: `Xk9$mWr4vLp2nBq7`

**⏱ Ожидание:** 2–4 минуты.

---

### Шаг 3.2 — Разрешить Docker без `sudo`

- [+] **Выполнено**

```bash
sudo usermod -aG docker deploy
```

Перелогиньтесь, чтобы изменения вступили в силу:

```bash
exit
su - deploy
```

---

### Шаг 3.3 — Проверить установку

- [+] **Выполнено**

```bash
docker --version && docker compose version
```

**✅ Результат:**
```
Docker version 27.x.x, build xxxxxxx
Docker Compose version v2.x.x
```

Финальная проверка:

```bash
docker run hello-world
```

**✅ Результат:** В выводе есть строка `Hello from Docker!`

---

### Шаг 3.4 — Установить Git

- [+] **Выполнено**

```bash
sudo apt install -y git
```

**✅ Результат:** Git установлен. Проверка: `git --version` покажет `git version 2.x.x`.

---

## Этап 4 — Привязка домена (DNS на Reg.ru)

> 📌 DNS-записи обновляются **от 15 минут до нескольких часов**. Настройте сейчас — а проверьте позже.

### Шаг 4.1 — Создать A-запись

- [+] **Выполнено**

1. Откройте **https://reg.ru** → войдите в аккаунт
2. В верхнем меню нажмите **«Мои домены»** (или «Домены и услуги»)
3. Нажмите на **имя вашего домена**
4. Перейдите на вкладку **«DNS-серверы и управление зоной»** (или «Зона DNS»)
5. Нажмите **«Добавить запись»** или найдите существующую A-запись для редактирования

6. Заполните поля:

   | Поле | Значение |
   |------|----------|
   | **Тип записи** | `A` |
   | **Subdomain / Имя** | `@` (означает корневой домен) |
   | **IP Address / Значение** | IP вашего сервера (например `185.104.123.456`) |
   | **TTL** | Оставьте по умолчанию |

7. Нажмите **«Сохранить»**

> ⚠️ Если уже есть A-запись с `@` — **отредактируйте** её. Не создавайте дубль.

---

### Шаг 4.2 — Проверить DNS (через 15–30 мин)

- [ ] **Выполнено**

На **вашем компьютере** (не на сервере!) откройте PowerShell:

```bash
nslookup opros-izdorov.ru
```

**✅ Результат:** В ответе — IP вашего сервера:
```
Name:    ваш-домен.ru
Address: 185.104.123.456
```

> Если IP ещё старый — подождите ещё 15–30 минут и повторите.

---

## Этап 5 — Загрузка проекта на сервер

> Два способа на выбор. **Способ A (Git)** — рекомендуемый, проще обновлять в будущем.

### Способ A — через Git (рекомендуемый)

#### Шаг 5.1 — Загрузить проект на GitHub

- [+] **Выполнено**

> Если проект уже на GitHub — сразу переходите к **Шагу 5.2**.

**На вашем компьютере** (терминал Cursor / PowerShell):

```bash
cd C:\Users\Nikola\Documents\Project\Opros
git init
git add .
git commit -m "Подготовка к деплою"
```

Затем создайте репозиторий на GitHub:

1. Откройте **https://github.com** → войдите
2. Нажмите **«+»** (правый верхний угол) → **«New repository»**
3. **Repository name:** `opros`
4. **Visibility:** `Private` ✅
5. Нажмите **«Create repository»**
6. Скопируйте URL (формат: `https://github.com/ВАШ_ЛОГИН/opros.git`)

Вернитесь в терминал:

```bash
git remote add origin https://github.com/ВАШ_ЛОГИН/opros.git
git branch -M main
git push -u origin main
```

> 📌 **Для Private-репозитория** GitHub попросит авторизацию.  
> Используйте **Personal Access Token** вместо пароля:  
> GitHub → ⚙️ Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token → галочка на `repo` → Generate → **скопируйте токен**.

---

#### Шаг 5.2 — Клонировать проект на сервер

- [+] **Выполнено**

**На сервере** (подключитесь: `ssh deploy@ВАШ_IP`):

```bash
cd /home/deploy
git clone https://github.com/ВАШ_ЛОГИН/opros.git
cd opros
```

> 📌 Для Private-репозитория: логин GitHub + Token вместо пароля.

**✅ Результат:** Проверьте, что файлы на месте:

```bash
ls -la
```

Вы увидите: `docker-compose.prod.yml`, `backend/`, `frontend/`, `nginx/` и другие файлы.

---

### Способ B — прямое копирование (без Git)

#### Шаг 5.1 (альтерн.) — Скопировать через SCP

- [ ] **Выполнено**

**На вашем компьютере:**

```bash
scp -r C:\Users\Nikola\Documents\Project\Opros deploy@ВАШ_IP:/home/deploy/opros
```

**⏱ Ожидание:** 1–5 минут.

---

## Этап 6 — Создание .env файла

### Шаг 6.1 — Открыть редактор

- [+] **Выполнено**

**На сервере:**

```bash
cd /home/deploy/opros
nano .env
```

> 📌 **Как пользоваться `nano`:**
> | Действие | Клавиши |
> |----------|---------|
> | Печатать текст | Просто печатайте |
> | Перемещение | Стрелки клавиатуры |
> | Вставить из буфера | Правая кнопка мыши или `Ctrl+Shift+V` |
> | Сохранить | `Ctrl + O` → `Enter` |
> | Выйти | `Ctrl + X` |

---

### Шаг 6.2 — Вставить содержимое

- [+] **Выполнено**

Скопируйте **весь блок ниже** и вставьте в nano.

> ⚠️ **Перед вставкой:** замените `ваш-домен.ru` на реальный домен в строках `CORS_ORIGINS_STR` и `FRONTEND_URL`.  
> Можно сначала вставить в блокнот на своём компьютере, сделать замену, а потом вставить в терминал.

```env
# ============================================
# PRODUCTION — Опросник пациента
# ============================================

# Общие
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=a3f8c1e9d4b7260f5e1a9c3d8b4f7e2a6d0c5b8e1f3a7d9c2b6e4f8a0d3c7b1

# PostgreSQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=opros_user
POSTGRES_PASSWORD=Pg_secur3_Opr0s_2026xQ
POSTGRES_DB=opros_db

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=R3d1s_Secur3_0pr0s_vK
SESSION_TTL=86400

# JWT
JWT_SECRET_KEY=7b2e4f6a8d0c3e5b1a9f7d4c6b8e2a0f3d5c7b9e1a4f6d8c0b2e4a6f8d0c3e5
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=48

# Битрикс24
BITRIX24_WEBHOOK_URL=https://izdorov.bitrix24.ru/rest/217109/z1oeeyul4xht5g33/
BITRIX24_INCOMING_TOKEN=245234asdufhb9oas94
BITRIX24_CATEGORY_ID=19

# Домен (ЗАМЕНИТЕ ваш-домен.ru НА РЕАЛЬНЫЙ ДОМЕН!)
CORS_ORIGINS_STR=https://opros-izdorov.ru
FRONTEND_URL=https://opros-izdorov.ru

# Админ-панель
ADMIN_USERNAME=opros_admin
ADMIN_PASSWORD=Adm1n_0proS_2026wZ

# Безопасность
RATE_LIMIT_PER_MINUTE=60
AUDIT_LOG_RETENTION_HOURS=24

# Docker
COMPOSE_PROJECT_NAME=opros
```

Сохраните: **`Ctrl + O`** → **`Enter`** → **`Ctrl + X`**

---

### Шаг 6.3 — Проверить файл

- [+] **Выполнено**

Убедитесь, что домен указан правильно:

```bash
grep "CORS_ORIGINS\|FRONTEND_URL" .env
```

**✅ Результат:** Должны отобразиться строки с вашим доменом:
```
CORS_ORIGINS_STR=https://opros-izdorov.ru
FRONTEND_URL=https://opros-izdorov.ru
```

> ❌ Если видите `ваш-домен.ru` — откройте файл: `nano .env` и замените на `opros-izdorov.ru`.

---

## Этап 7 — Настройка Nginx под домен

### Шаг 7.1 — Убедиться, что конфиг оригинальный

- [+] **Выполнено**

```bash
cd /home/deploy/opros
grep "ssl_certificate" nginx/conf.d/default.conf
```

**✅ Результат:** Должны появиться строки с `ssl_certificate` (2 строки).

> ❌ **Если ничего не вывело** — значит файл содержит временный конфиг (только HTTP).
> Восстановите оригинал одним из способов:
>
> **Способ 1** — из бэкапа (если делали `cp ... .backup`):
> ```bash
> cp nginx/conf.d/default.conf.backup nginx/conf.d/default.conf
> ```
>
> **Способ 2** — из Git (если бэкапа нет):
> ```bash
> git checkout nginx/conf.d/default.conf
> ```

---

### Шаг 7.2 — Заменить домен одной командой

- [+] **Выполнено**

Вместо ручного редактирования в nano — выполните **одну команду** `sed`, которая заменит все 4 места разом:

```bash
sed -i 's/server_name _;/server_name opros-izdorov.ru;/g; s/your-domain.ru/opros-izdorov.ru/g' nginx/conf.d/default.conf
```

> 📌 **Что делает:** Заменяет `server_name _;` → `server_name opros-izdorov.ru;` и `your-domain.ru` → `opros-izdorov.ru` во всём файле.

---

### Шаг 7.3 — Проверить изменения

- [+]Выполнено**

```bash
grep -E "server_name|ssl_certificate" nginx/conf.d/default.conf
```

**✅ Результат:**
```
    server_name opros-izdorov.ru;
    server_name opros-izdorov.ru;
    ssl_certificate /etc/letsencrypt/live/opros-izdorov.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/opros-izdorov.ru/privkey.pem;
```

> ❌ Если видите `server_name _;` или `your-domain.ru` — повторите команду `sed` из шага 7.2.

---

## Этап 8 — Получение SSL-сертификата

> ⚠️ **Перед этим этапом** убедитесь, что DNS обновился (Шаг 4.2 — `nslookup` показывает IP сервера).

### Шаг 8.1 — Подготовить директории

- [+] **Выполнено**

```bash
cd /home/deploy/opros
mkdir -p certbot/conf certbot/www
```

---

### Шаг 8.2 — Создать временный конфиг Nginx (только HTTP)

- [+] **Выполнено**

Сохраните оригинал и создайте упрощённый конфиг:

```bash
cp nginx/conf.d/default.conf nginx/conf.d/default.conf.backup
```

```bash
cat > nginx/conf.d/default.conf << 'NGINX_TEMP'
server {
    listen 80;
    listen [::]:80;
    server_name opros-izdorov.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Server is running';
        add_header Content-Type text/plain;
    }
}
NGINX_TEMP
```

---

### Шаг 8.3 — Запустить Nginx

- [+] **Выполнено**

```bash
docker volume create opros_frontend_build
docker compose -f docker-compose.prod.yml up -d nginx
```

Проверьте:

```bash
docker ps
```

**✅ Результат:** Контейнер `opros-nginx` в статусе `Up`:
```
CONTAINER ID   IMAGE          STATUS   PORTS                                      NAMES
xxxxxxxxxxxx   nginx:alpine   Up       0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp   opros-nginx
```

---

### Шаг 8.4 — Получить SSL-сертификат

- [ ] **Выполнено**

```bash
docker compose -f docker-compose.prod.yml run --rm --entrypoint '' certbot \
  certbot certonly \
  --webroot \
  --webroot-path=/var/www/certbot \
  --email prikolyank@gmail.com \
  --agree-tos \
  --no-eff-email \
  -d opros-izdorov.ru
```

> 📌 **Важно:** Флаг `--entrypoint ''` обязателен — без него compose использует entrypoint из конфигурации (цикл `certbot renew`), и команда `certonly` не выполнится.

**✅ Результат:**
```
Congratulations! Your certificate and chain have been saved at:
/etc/letsencrypt/live/opros-izdorov.ru/fullchain.pem
```

> ❌ Ошибка `DNS problem: NXDOMAIN` → DNS ещё не обновился, подождите и повторите.  
> ❌ Ошибка `Connection refused` → проверьте `docker ps`, Nginx должен быть запущен.

---

### Шаг 8.5 — Восстановить полный конфиг Nginx

- [ ] **Выполнено**

```bash
cp nginx/conf.d/default.conf.backup nginx/conf.d/default.conf
docker compose -f docker-compose.prod.yml down
```

---

## Этап 9 — Запуск проекта

### Шаг 9.1 — Собрать и запустить все сервисы

- [ ] **Выполнено**

```bash
cd /home/deploy/opros
docker compose -f docker-compose.prod.yml up -d --build
```

> 📌 Собирает Docker-образы backend и frontend, запускает PostgreSQL, Redis, Nginx, Certbot.

**⏱ Ожидание:** 3–10 минут (первая сборка самая долгая).

---

### Шаг 9.2 — Проверить статус контейнеров

- [ ] **Выполнено**

```bash
docker compose -f docker-compose.prod.yml ps
```

**✅ Результат:**

```
NAME                     STATUS              PORTS
opros-nginx              Up                  0.0.0.0:80->80, 0.0.0.0:443->443
opros-backend            Up (healthy)        8000/tcp
opros-postgres           Up (healthy)        5432/tcp
opros-redis              Up (healthy)        6379/tcp
opros-frontend-builder   Exited (0)          
opros-certbot            Up
```

> 📌 `opros-frontend-builder` в статусе `Exited (0)` — это **нормально**! Он собирает фронтенд и завершается.

> ❌ Если контейнер не запустился — смотрите логи:
> ```bash
> docker compose -f docker-compose.prod.yml logs имя-сервиса
> ```

---

### Шаг 9.3 — Выполнить миграции базы данных

- [ ] **Выполнено**

```bash
docker compose -f docker-compose.prod.yml exec -e PYTHONPATH=/app backend alembic upgrade head
```
INFO  [alembic.runtime.migration] Running upgrade 001 -> 002, ...
INFO  [alembic.runtime.migration] Running upgrade 002 -> 003, ...
```

---

## Этап 10 — Проверка работоспособности

### Шаг 10.1 — Проверить сайт в браузере

- [ ] **Выполнено**

1. Откройте браузер на **вашем компьютере**
2. Перейдите по адресу: **`https://opros-izdorov.ru`**
3. Вы должны увидеть **интерфейс опросника** с экраном согласия (152-ФЗ)
4. В адресной строке должен быть 🔒 **замок** (SSL работает)

---

### Шаг 10.2 — Проверить API

- [ ] **Выполнено**

**На сервере:**

```bash
curl -s http://localhost:8000/health | head -20
```

**✅ Результат:** JSON с информацией о статусе или HTTP 200.

---

### Шаг 10.3 — Проверить админ-панель

- [ ] **Выполнено**

1. Откройте в браузере: **`https://opros-izdorov.ru/admin/`**
2. Введите:
   - **Логин:** `opros_admin`
   - **Пароль:** `Adm1n_0proS_2026wZ`
3. Вы должны увидеть интерфейс администратора

---

### Шаг 10.4 — Проверить базу и кэш

- [ ] **Выполнено**

```bash
docker compose -f docker-compose.prod.yml exec postgres pg_isready
docker compose -f docker-compose.prod.yml exec redis redis-cli -a R3d1s_Secur3_0pr0s_vK ping
```

**✅ Результат:**
```
/var/run/postgresql:5432 - accepting connections
PONG
```

---

## 🎉 Деплой завершён!

Проект работает по адресу **`https://opros-izdorov.ru`** 🚀

---

## 📖 Приложение A — Обновление проекта

Когда вы внесли изменения в код и хотите обновить сервер:

**На вашем компьютере:**

```bash
cd C:\Users\Nikola\Documents\Project\Opros
git add .
git commit -m "Описание изменений"
git push
```

**На сервере** (`ssh deploy@ВАШ_IP`):

```bash
cd /home/deploy/opros
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

Если изменились модели БД:

```bash
docker compose -f docker-compose.prod.yml exec -e PYTHONPATH=/app backend alembic upgrade head
```

---

## 📖 Приложение B — Шпаргалка команд

| Действие | Команда |
|----------|---------|
| Запустить | `docker compose -f docker-compose.prod.yml up -d` |
| Остановить | `docker compose -f docker-compose.prod.yml down` |
| Пересобрать | `docker compose -f docker-compose.prod.yml up -d --build` |
| Статус | `docker compose -f docker-compose.prod.yml ps` |
| Все логи | `docker compose -f docker-compose.prod.yml logs -f` |
| Логи сервиса | `docker compose -f docker-compose.prod.yml logs backend` |
| Перезапуск | `docker compose -f docker-compose.prod.yml restart backend` |
| Миграции | `docker compose -f docker-compose.prod.yml exec -e PYTHONPATH=/app backend alembic upgrade head` |
| Зайти в контейнер | `docker compose -f docker-compose.prod.yml exec backend bash` |
| SSH на сервер | `ssh deploy@ВАШ_IP` |

---

## 📖 Приложение C — Решение проблем

### ❌ Контейнер не запускается

```bash
docker compose -f docker-compose.prod.yml logs имя-сервиса
docker compose -f docker-compose.prod.yml restart имя-сервиса
```

### ❌ Ошибка 502 Bad Gateway

Backend не отвечает:

```bash
docker compose -f docker-compose.prod.yml ps backend
docker compose -f docker-compose.prod.yml logs backend
docker compose -f docker-compose.prod.yml restart backend
```

### ❌ Ошибка подключения к БД

Проверьте совпадение пароля:

```bash
grep POSTGRES_PASSWORD .env
```

### ❌ SSL-сертификат истёк

```bash
docker compose -f docker-compose.prod.yml run --rm certbot renew --force-renewal
docker compose -f docker-compose.prod.yml restart nginx
```

> 📌 Certbot обновляет сертификат **автоматически** каждые 12 часов.

### ❌ Нехватка памяти (если RAM < 4 ГБ)

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### ❌ Полная переустановка (крайний случай)

> ⚠️ **Удалит базу данных и все данные!**

```bash
cd /home/deploy/opros
docker compose -f docker-compose.prod.yml down -v
docker compose -f docker-compose.prod.yml up -d --build
docker compose -f docker-compose.prod.yml exec backend alembic upgrade head
```+